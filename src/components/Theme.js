// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#0A092D', // Set your desired primary color here
        },
        secondary: {
            main: '#F50057', // You can set the secondary color too
        },
    },
});

export default theme;
